def foo(a):
    """foo function, it returns its argument.

    Parameters
    ----------
    a : any type
        The input argument to be returned.

    Returns
    -------
    any type
        The same value as the input argument `a`.
    """
    return a
